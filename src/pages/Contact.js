import React from 'react';
import { Button, Card } from 'react-bootstrap';

const Contact = () => {
    return (
        <div>
            <h1>
                Contact page
            </h1>
        </div>
    );
};

export default Contact;